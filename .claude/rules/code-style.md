TypeScript strict no-any. cn() utility for class merging. Server components default, client only when needed. next/image for all images. next/link for all navigation. next/font/google for fonts (Outfit, Plus Jakarta Sans, JetBrains Mono — never use CDN or generic fonts). Framer Motion for animations. Data in src/data/. Named exports for components, default exports for pages. Max 200 lines per component. Colors: primary (indigo), accent (cyan), surface (dark). All services must gracefully degrade when env vars are missing.
