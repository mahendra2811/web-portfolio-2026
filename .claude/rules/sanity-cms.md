Sanity CMS: Project ID 9lxe9yiz, dataset production. Free plan (10K docs, no Functions, no AI credits). Read client: src/lib/sanity/client.ts (next-sanity, CDN in prod). Write operations via scripts/blog.ts using @sanity/client with SANITY_API_TOKEN (Editor role). Schema: post (title, slug, excerpt, coverImage, body as PortableText, categories as references, publishedAt, readingTime) and category (title, slug, description). Queries in src/lib/sanity/queries.ts. Blog posts fall back to src/data/blog-placeholder.ts when Sanity is unavailable. ISR revalidation: blog list = 60s, blog post = 3600s. Webhook for cache invalidation at /api/revalidate. Studio accessible at /studio. Never upload images to Sanity on free plan — use external URLs instead.
