Glass components use globals.css classes (.glass, .glass-card, .glass-button). Animation pattern: use ScrollReveal/StaggerList/TextReveal wrappers from src/components/motion/ — don't write raw motion.div variants inline. Use presets from src/lib/motion.ts (fadeInUp, staggerContainer, scaleIn, slideInLeft/Right). Three.js: lazy load with next/dynamic ssr:false, mobile gets gradient-mesh fallback via useMediaQuery hook. Form pattern: validation, loading state, toast feedback, graceful fallback if services unavailable. All images use picsum.photos placeholders until real assets are provided.
